name: Build and Test

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build project
      run: npm run build

    - name: Run tests
      run: npm test
      continue-on-error: true

  build-electron-app:
    needs: build
    runs-on: windows-latest
    if: github.event_name == 'push'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build Electron app
      run: npm run build:win
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Get version from package.json
      id: version
      run: |
        $version = (Get-Content package.json | ConvertFrom-Json).version
        $commitSha = "${{ github.sha }}".Substring(0, 7)
        $branchName = "${{ github.ref_name }}" -replace '[^a-zA-Z0-9_-]', '-'
        echo "version=$version" >> $env:GITHUB_OUTPUT
        echo "short_sha=$commitSha" >> $env:GITHUB_OUTPUT
        echo "branch=$branchName" >> $env:GITHUB_OUTPUT

    - name: Upload artifacts (Develop/PR)
      if: github.event_name == 'pull_request' || github.ref != 'refs/heads/main' && github.ref != 'refs/heads/master'
      uses: actions/upload-artifact@v4
      with:
        name: windows-build-dev-${{ steps.version.outputs.short_sha }}
        path: dist/
        retention-days: 7

    - name: Upload artifacts (Release)
      if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
      uses: actions/upload-artifact@v4
      with:
        name: windows-build-release-${{ steps.version.outputs.version }}
        path: dist/

    - name: Create Release
      if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ steps.version.outputs.version }}
        name: Release v${{ steps.version.outputs.version }}
        body: |
          ## 自动构建版本
          
          ### 更新内容
          - 版本：${{ steps.version.outputs.version }}
          - 构建时间：${{ github.event.head_commit.timestamp }}
          - Commit：${{ github.sha }}
          
          ### 安装文件
          - Windows 安装版：`公式可视化 Setup ${version}.exe`
          - Windows 便携版：`公式可视化 ${version}.exe`
        files: |
          dist/*.exe
          dist/*.zip
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
